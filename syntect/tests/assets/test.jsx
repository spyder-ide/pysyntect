
import React from 'React';

export default class ThisCompoment extends React.Component {
    state = {
        key: "value"
    }

    render() {
        const variable = 2;
        var x = 2;
        const {key1: k1, key2: k2} = u;
        [...a, b];
        let f = (x, y) => {
            return x + y;
        }
        let a = "${variable}";
        return (
            <Holi keys={x}></Holi>
        )
    }
}